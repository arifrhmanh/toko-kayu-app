name: frontend
description: "Kayu Adi - Wood Trading Application"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # State Management
  provider: ^6.1.2
  
  # HTTP & API
  dio: ^5.4.3+1
  
  # Storage
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.2.3
  
  # Supabase for Realtime
  supabase_flutter: ^2.5.7
  
  # UI Components
  google_fonts: ^6.2.1
  flutter_spinkit: ^5.2.1
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  fl_chart: ^0.68.0
  
  # WebView for Midtrans
  webview_flutter: ^4.8.0
  
  # Utils
  intl: ^0.19.0
  image_picker: ^1.1.2
  url_launcher: ^6.3.0
  
  # Icons
  iconsax: ^0.0.8
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
